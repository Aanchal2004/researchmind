# Risks

- API rate limits -> cache and fallback
- hallucinated citations -> synthesize only from source text
- cold starts -> keep backend lightweight
- schema drift -> isolate adapters behind MCP tools
- scope creep -> finish one slice before adding another
